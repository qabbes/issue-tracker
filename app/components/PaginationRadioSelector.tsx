import { Flex, RadioGroup, Text } from "@radix-ui/themes";
//import { pageSizes } from "./PaginationSizeSelector";

const PaginationRadioSelector = () => {
  /* return (
    <Flex gap="2">
      <Text size="2">Items per page:</Text>
      <Flex direction="row" asChild gap="2">
        <RadioGroup.Root size="2" defaultValue="1">
          {pageSizes.map((pageSize) => (
            <RadioGroup.Item value={pageSize.value}>{pageSize.label}</RadioGroup.Item>
          ))}
        </RadioGroup.Root>
      </Flex>
    </Flex>
  ); */
};

export default PaginationRadioSelector;
